# Prisma D1 Configuration
# Add this to your wrangler.jsonc or environment

# The D1 database binding is configured in wrangler.jsonc
# Prisma will use the D1 adapter to connect to your database

# For local development, ensure D1 is properly set up with:
# wrangler d1 create <database-name>
# wrangler d1 info <database-name>
